$(document).ready(function() {
  $("#update_header").on("click", function() {
    // Select the <header> element by its ID and update its text content
    $("header").text("New Header!!!");
  });
});
